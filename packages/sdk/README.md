# @shadeotc/sdk

TypeScript SDK for ShadeOTC. Exposes ZK prover/verifier helpers, Solana helpers, and an Anchor on-chain verifier client.

Quick start:

```bash
pnpm install
pnpm --filter @shadeotc/sdk build
```

Build Noir artifacts:

```bash
sh ../../scripts/build-noir.sh
```
