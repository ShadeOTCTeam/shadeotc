# Quickstart

1. Install dependencies: `pnpm install`
2. Build SDK: `pnpm -w build`
3. Build Noir artifacts: `sh scripts/build-noir.sh`
4. Run example server: `pnpm --filter examples/server-express dev`

See `examples` for complete sample code.
